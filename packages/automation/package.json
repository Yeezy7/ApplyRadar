{
  "name": "@applyradar/automation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/sidecar.ts --bundle --platform=node --format=esm --outfile=dist/sidecar.mjs --external:playwright",
    "dev": "tsx src/sidecar.ts"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
